services:
  # SERVICIO 1: BACKEND API (Node.js)
  - type: web
    name: mi-api-backend
    runtime: node
    region: oregon
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: mi-api-db
          property: connectionString
  
  # SERVICIO 2: FRONTEND (Static Site)
  - type: web
    name: mi-api-frontend
    runtime: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    headers:
      - path: /*
        name: X-Frame-Options
        value: sameorigin

# BASE DE DATOS (sección separada)
databases:
  - name: mi-api-db
    plan: free
    databaseName: apidb
    user: admin
    region: oregon
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all IP addresses